{
  "name": "team-collaboration-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
    "lint": "npm --prefix frontend run lint && npm --prefix backend run lint",
    "test": "npm --prefix frontend run test && npm --prefix backend run test",
    "format": "npm --prefix frontend run format",
    "format:fix": "npm --prefix frontend run format:fix",
    "seed": "npm --prefix backend run seed"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
